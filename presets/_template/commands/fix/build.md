---
description: Diagnose and fix build errors
inclusion: manual
argument-hint: "[error-context]"
---

## Arguments
CONTEXT: $1 (default: empty)

## Workflow
1. Run build and capture error output
2. Analyze compilation or bundling errors
3. Identify root cause (missing deps, type errors, config issues)
4. Apply fixes and verify build succeeds
