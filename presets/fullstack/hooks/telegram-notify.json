{
  "version": "v1",
  "hooks": [
    {
      "name": "Telegram Notify",
      "description": "Sends a Telegram notification when the agent finishes. No-ops unless TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID are set.",
      "enabled": true,
      "trigger": "Stop",
      "action": {
        "type": "command",
        "command": "node .kiro/hooks/telegram-notify.js"
      },
      "timeout": 15
    }
  ]
}
