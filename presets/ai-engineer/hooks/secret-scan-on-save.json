{
  "version": "v1",
  "hooks": [
    {
      "name": "Secret Scan on Save",
      "description": "When a code or config file is saved, scan it for hardcoded secrets. Scoped to file writes so it does not run on every tool call.",
      "enabled": false,
      "trigger": "PostFileSave",
      "matcher": "(^(src|lib|app)/.+|\\.(env|ya?ml|json|tf|tfvars))$",
      "action": {
        "type": "agent",
        "prompt": "The saved file changed. Inspect it for hardcoded secrets: API keys, access tokens, passwords, private keys, and database connection strings with embedded credentials. If any are found, report the exact location and a safe alternative (environment variable via .env). Do not modify unrelated files."
      }
    }
  ]
}
