---
description: Run API tests for endpoint validation and contract verification.
argument-hint:
  - endpoint-or-scope
---

Run API tests for the specified endpoints or service scope.

## Usage
Specify an endpoint path, service name, or test file. Leave empty to run all API tests.
